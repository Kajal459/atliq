// Gmail connector removed. Quick capture (paste-in notes) plus the
// Resend-based daily digest cover the same ground without an OAuth consent
// screen, Google Cloud project, or verification step to maintain.
//
// This file - and the rest of src/lib/gmail/, plus src/app/api/gmail/ - is
// left as an empty placeholder because the sandbox this edit was made from
// can't delete files on this mount (a recurring FUSE limitation). Safe to
// delete both folders entirely from your own Terminal/Finder:
//   rm -rf "src/lib/gmail" "src/app/api/gmail"
export {};
